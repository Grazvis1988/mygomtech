export enum Routes {
  Login = '/login',
  Users = '/items',
  Wrong = '/items/wrong',
  Reused = '/items/reused',
  Old = '/items/old',
  Root = '/',
}

export enum API {
  Logout = 'api/logout',
  Login = 'api/login',
  Items = 'api/items',
  User = 'api/user',
}

export enum Roles {
  read = 'read',
  write = 'write',
  amin = 'amin'
}

